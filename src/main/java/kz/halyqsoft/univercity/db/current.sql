CREATE TABLE user_arrival (
  id      BIGINT    NOT NULL,
  user_id BIGINT    NOT NULL,
  created TIMESTAMP NOT NULL
);

ALTER TABLE ONLY user_arrival
  ADD CONSTRAINT pk_user_arrival PRIMARY KEY (id);

ALTER TABLE ONLY user_arrival
  ADD CONSTRAINT fk_user_arrival_users FOREIGN KEY (user_id)
REFERENCES users (id) ON UPDATE RESTRICT ON DELETE RESTRICT;

CREATE SEQUENCE S_USER_ARRIVAL
MINVALUE 0
START WITH 1
NO CYCLE;

CREATE SEQUENCE S_NEWS
MINVALUE 0
START WITH 1
NO CYCLE;